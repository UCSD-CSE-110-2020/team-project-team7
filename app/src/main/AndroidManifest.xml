<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.walkwalkrevolution">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Walk Walk Revolution"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HeightForm" />
        <activity
            android:name=".RoutesForm"
            android:exported="true" />
        <activity android:name=".SetDate" />
        <activity android:name=".NotesPage" />
        <activity android:name=".RoutesList" />
        <activity android:name=".WalkRunSession" />
        <activity android:name=".MockPage" />
        <activity android:name=".HomePage" android:launchMode="singleInstance">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <service
            android:name="MyFirebaseMessagingService">
            <intent-filter>
                <action android:name=".com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>
